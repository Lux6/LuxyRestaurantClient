<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mvila.cat.luxyrestaurantclient">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ComandaClient" />
        <activity android:name=".CrearComanda" />
        <activity android:name=".EditarComanda" />
        <activity android:name=".EspaiClient" />
        <activity android:name=".AfegirItemComanda" />
        <activity android:name=".LogInTreballador" />
        <activity android:name=".EspaiTreballador" />
        <activity android:name=".FinalitzarComanda" />
        <activity android:name=".OpinarComanda"></activity>
    </application>

</manifest>